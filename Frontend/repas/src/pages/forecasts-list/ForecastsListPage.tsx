/**
 * Page liste des prévisions — shell vide.
 * Sera remplie à la Phase 4 (tableau paginé branché sur `forecastStore.fetchList`).
 */
export function ForecastsListPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold">Prévisions</h1>
      <p className="mt-2 text-muted-foreground">
        Liste des prévisions (à venir : Phase 4).
      </p>
    </div>
  );
}
