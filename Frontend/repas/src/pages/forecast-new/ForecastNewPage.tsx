/**
 * Page création d'une prévision — shell vide.
 * Sera remplie à la Phase 5 (upload 2 CSV + stock_tampon → `forecastStore.create`).
 */
export function ForecastNewPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold">Nouvelle prévision</h1>
      <p className="mt-2 text-muted-foreground">
        Formulaire d'upload (à venir : Phase 5).
      </p>
    </div>
  );
}
