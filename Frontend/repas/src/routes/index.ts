/**
 * Point d’entrée public du module routes.
 * Édition courante : `paths.ts` + `routes.config.tsx` à la racine de ce dossier.
 */
export { PATHS, type AppPath } from "@/routes/paths";
export { AppRoutes } from "@/routes/internals/AppRoutes";
export type { AppRouteEntry } from "@/routes/app-route-entry";
